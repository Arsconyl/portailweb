export default interface Article {
  title: string;
  content: string;
  date: string;

  // constructor(title: string, content: string) {
  //   this.title = title;
  //   this.content = content;
  // }
}
